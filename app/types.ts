/** ISO 3166-1 alpha-2 codes of supported countries. */
export type Country = 'CA' | 'MX' | 'US';

/** BCP 47 language-region tags (ISO 639-1 + ISO 3166-1 alpha-2) of supported locales. */
export type Locale =
  | 'en-CA'
  | 'en-US'
  | 'es-MX'
  | 'es-US'
  | 'fr-CA';
